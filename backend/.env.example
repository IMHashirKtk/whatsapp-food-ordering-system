# Browser origin allowed to call the API and connect to Socket.IO.
# Use http://localhost:3000 for local development and the deployed dashboard
# origin in production, for example https://dashboard.example.com.
DASHBOARD_ORIGIN=http://localhost:3000
